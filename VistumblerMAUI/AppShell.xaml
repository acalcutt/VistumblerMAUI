<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:VistumblerMAUI.Views"
       x:Class="VistumblerMAUI.AppShell"
       Shell.FlyoutBehavior="Disabled"
       Title="Vistumbler">

    <TabBar>
        <ShellContent Title="Scan"
                      Icon="tab_scan.png"
                      ContentTemplate="{DataTemplate views:ScanPage}"
                      Route="ScanPage" />
        <ShellContent Title="APs"
                      Icon="tab_list.png"
                      ContentTemplate="{DataTemplate views:AccessPointListPage}"
                      Route="AccessPointListPage" />
        <ShellContent Title="Map"
                      Icon="tab_map.png"
                      ContentTemplate="{DataTemplate views:MapPage}"
                      Route="MapPage" />
        <ShellContent Title="Settings"
                      Icon="tab_settings.png"
                      ContentTemplate="{DataTemplate views:SettingsPage}"
                      Route="SettingsPage" />
    </TabBar>

</Shell>
