<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.vistumbler.mauiapp">

    <!-- WiFi scanning — mirrors vistumbler-android AndroidManifest.xml permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required on Android 10+ for background scanning -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Internet for OUI lookup / tile downloads -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.wifi"                  android:required="false" />
    <uses-feature android:name="android.hardware.location.gps"          android:required="false" />
    <uses-feature android:name="android.hardware.location.network"      android:required="false" />

    <!--
        No <activity> element here on purpose: MainActivity.cs's [Activity] attribute
        is what the Android build merges into the final manifest (theme, configChanges,
        launchMode, intent-filter, etc). Hand-writing it here too just creates a class
        the build never compiles, since there's nothing generating it from C#.
    -->
    <application android:allowBackup="true"
                 android:label="Vistumbler"
                 android:supportsRtl="true">
    </application>
</manifest>
